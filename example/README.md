# ResponsiveFlexList Example

This example app demonstrates the package's main layout modes:

- Static children rendered as a responsive list/grid.
- Dynamic builder usage with `ResponsiveFlexGridDelegate`.
- Row and column separators, including round-robin layout.
- Pinterest-style masonry with loading progress.
- Custom breakpoint behavior.

Run it with:

```bash
flutter run
```
